;; SudoLang has no in-grammar fenced code blocks. When a markdown file
;; contains a `sudo` or `SudoLang` code fence, Zed's markdown extension
;; injects this grammar into the fence based on the language tag.
;;
;; This file intentionally has no captures.
